import { Input } from "@/shared/components/ui/Input";

export function CitySearchInput(props) {
  return <Input id="city-search" label="City search" placeholder="Search destinations" {...props} />;
}

